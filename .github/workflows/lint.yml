name: Linter

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

permissions:
  contents: write

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, xml, dom, fileinfo, curl, tokenizer, bcmath
          ini-values: memory_limit=-1
          coverage: none

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: |
          # Install PHP dependencies
          composer install --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist -v
          
          # Generate lock file if missing (fallback)
          if [ ! -f package-lock.json ]; then
            echo "Generating package-lock.json..."
            npm install --package-lock-only
          fi
          
          # Install Node dependencies
          npm ci --no-audit

      - name: Run Pint
        run: vendor/bin/pint --test

      - name: Check Frontend Formatting
        run: npm run format:check

      - name: Lint Frontend
        run: npm run lint

      # Uncomment only if you want automatic fixes
      # - name: Commit Changes
      #   if: success()
      #   uses: stefanzweifel/git-auto-commit-action@v5
      #   with:
      #     commit_message: 'style: auto-fix linting issues'
      #     commit_options: '--no-verify'